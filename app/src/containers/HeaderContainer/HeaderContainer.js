import { connect } from 'react-redux'

import ReactComponent from '../../components/Header/'
import {} from '../../actions/'

export default connect(
    state => ({}),
    dispatch => ({})
)(ReactComponent)