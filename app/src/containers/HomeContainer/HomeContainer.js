import { connect } from 'react-redux'

import ReactComponent from '../../components/Home/'
import {} from '../../actions/'

export default connect(
    state => ({}),
    dispatch => ({})
)(ReactComponent)