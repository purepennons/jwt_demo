import { connect } from 'react-redux'

import ReactComponent from '../../components/Footer/'
import {} from '../../actions/'

export default connect(
    state => ({}),
    dispatch => ({})
)(ReactComponent)